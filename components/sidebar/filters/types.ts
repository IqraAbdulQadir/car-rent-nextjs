export interface TypeItemProps {
  icon: string;
  label: string;
  count: number;
  isActive?: boolean;
}

export interface CapacityItemProps {
  icon: string;
  persons: string;
  count: number;
  isActive?: boolean;
}
